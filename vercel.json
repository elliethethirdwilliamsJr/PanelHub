{
  "buildCommand": "pnpm run build",
  "outputDirectory": "artifacts/paneldrop/dist",
  "installCommand": "pnpm install",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/.*", "dest": "/api/$0" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
